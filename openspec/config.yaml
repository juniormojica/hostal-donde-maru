# OpenSpec — SDD Configuration
# Auto-generated by sdd-init for hostal-donde-maru

context: |
  Landing page for Hostal Donde Maru, a React 18 + Vite 6 SPA (SWC).
  Tailwind CSS 3 styling, Google Maps API integration, Framer Motion animations.
  No TypeScript, no routing, no state management library. Component-per-directory pattern.
  ESLint 9 flat config (react, react-hooks, react-refresh). No test runner installed.

strict_tdd: false
status: bootstrap

testing:
  runner:
    command: ""
    framework: ""
    available: false
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
    command: ""
  quality:
    linter:
      available: true
      command: eslint .
    type_checker:
      available: false
      command: ""
    formatter:
      available: false
      command: ""

phases:
  explore:
    requires_test: false
  propose:
    requires_test: false
  spec:
    requires_test: false
  design:
    requires_test: false
  tasks:
    requires_test: false
  apply:
    requires_test: false
  verify:
    requires_test: false
  archive:
    requires_test: false

persistence:
  mode: hybrid
  artifact_store: both
  chained_pr_strategy: force-chained
  review_budget_lines: 400
